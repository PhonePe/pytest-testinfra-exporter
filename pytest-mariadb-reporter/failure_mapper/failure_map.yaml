error_maps:

  - match_type: exact
    target_logs:
      - "Access denied for user"
    defect:
      name: "DB Access Issues"

  - match_type: regex
    target_logs:
      - "host.network.check_connectivity.*"
    defect:
      name: "Connectivity Missing"
